Coloque aqui os manuais técnicos usando o código como nome do arquivo, em
maiúsculo, extensão `.PDF` — ex.: `MB-001.PDF`, `CPU-001.PDF`.

O site já procura o arquivo automaticamente pelo código, sem precisar mexer
na planilha nem rodar `build.py`. Só salvar o arquivo aqui e recarregar a
página. (Detalhes e exceções no README principal da pasta do site.)
